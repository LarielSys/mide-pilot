# MIDE Cockpit — Session Handoff
**Date:** 2026-05-06  
**Worker:** ubuntu-worker-01 (ubuntu-atlas-01)  
**Operator:** admin-lariel  

---

## What Was Accomplished

### 1. Ollama Containerized with GPU Dedication
- Stopped systemd Ollama (`sudo systemctl stop ollama && sudo systemctl disable ollama`)
- Created `pilot_v1/customide/docker-compose.yml` with Ollama using `runtime: nvidia` + `network_mode: host`
- GPU: RTX 5060 Ti (UUID `GPU-dc07154d-a52e-2970-7237-e6f52c1de5ea`, 16GB VRAM)
- Models loaded: `qwen2.5:14b`, `qwen2.5:7b`, `kimi-k2.6:cloud`
- Volume: Docker named volume `ollama` (external, at `/var/lib/docker/volumes/ollama/_data`)
- **Note:** `network_mode: host` required because `runtime: nvidia` is incompatible with Docker bridge networking due to NVML version mismatch (kernel `580.126.09` vs library `580.142`). Reboot will resolve mismatch permanently.

### 2. Full Stack Dockerized
| Service | Container | Port | Image |
|---------|-----------|------|-------|
| Ollama LLM | `mide-ollama` | 11434 (host) | `ollama/ollama:latest` |
| FastAPI backend | `mide-backend` | 5555 | `mide-backend:latest` |
| Static frontend | `mide-frontend` | 5570 | `mide-frontend:latest` |

- `docker compose up -d` from `pilot_v1/customide/` starts the full stack
- Backend reaches Ollama at `http://172.17.0.1:11434` (Docker bridge gateway to host)

### 3. Bug Fixes Applied
| File | Fix |
|------|-----|
| `frontend/js/config.js` line 16 | Syntax corruption `here is mode:` → `mode:` |
| `frontend/js/app.js` line 1694 | Hardcoded model `qwen2.5-coder:14b` → `qwen2.5:14b` |
| `frontend/js/app.js` line 1413 | Thread tag `qwen2.5-coder:14b` → `qwen2.5:14b` |
| `frontend/index.html` line 92 | Display tag updated to `qwen2.5:14b` |
| `backend/app/routes/shared_llm.py` line ~120 | Default model now reads `CUSTOMIDE_OLLAMA_MODEL` env var |
| `backend/app/routes/runtime.py` line 244 | Events log slice `[-40:]` → `[:40]` (file is newest-first) |
| `pilot_v1/config/worker1_services.json` | `model_primary` updated to `qwen2.5:14b` |

### 4. Backend Git Sync Health Fixed
- Installed `git` in `mide-backend` Dockerfile
- Added `safe.directory /pilot_v1/customide` to Dockerfile (`RUN git config --global`)
- Mounted `.git` read-only into backend container: `/home/larieladmin/mide-pilot/.git:/pilot_v1/customide/.git:ro`
- Cockpit now shows: `branch: main`, `heads_match: true`, live commit SHA

### 5. Autopilot Status
- Process running: PID 3254, `worker_mtask_autopilot.sh --worker-id=ubuntu-worker-01 --poll-seconds=60`
- Repo path: `/home/larieladmin/Documents/itheia-llm/MIDE` → symlink → `/home/larieladmin/mide-pilot`
- Heartbeats pushing every 60s to `github.com/LarielSys/mide-pilot`
- **Currently idle:** `eligible=0` — all 117 approved tasks have result files
- 99 completed, 18 failed (mostly legacy failures — port 8091 unreachable, stale conditions)
- New tasks execute automatically on next poll after `status: approved_to_execute` + no result file

### 6. MTASK-0107 Created and Executed
- **Task:** Generate living architecture snapshot of the cockpit for brain context
- **Output:** `pilot_v1/state/cockpit_architecture.md` (256 lines)
- Covers: directory tree, Docker topology, API routes, chat flow, pane data sources, state files, Ollama models
- Executed successfully by autopilot

---

## Pending / Next Steps

### Immediate
- [ ] **Ollama GPU tuning** — add these env vars to `docker-compose.yml` Ollama service and restart:
  ```yaml
  - OLLAMA_FLASH_ATTENTION=1
  - OLLAMA_KV_CACHE_TYPE=q8_0
  - OLLAMA_NUM_PARALLEL=4
  - OLLAMA_MAX_LOADED_MODELS=2
  - OLLAMA_KEEP_ALIVE=30m
  - OLLAMA_GPU_OVERHEAD=0
  ```
  Then: `docker compose up -d --force-recreate ollama`

### Recommended
- [ ] Reboot machine to resolve NVML version mismatch (`580.126.09` vs `580.142`) — will enable full NVIDIA toolkit features
- [ ] Review 18 failed MTASKs — some may need retry with updated executor scripts
- [ ] Add new MTASKs for remaining work (assign `status: approved_to_execute`, autopilot will pick up)

---

## Key File Locations

| What | Path |
|------|------|
| Full stack compose | `pilot_v1/customide/docker-compose.yml` |
| Backend Dockerfile | `pilot_v1/customide/backend/Dockerfile` |
| Frontend Dockerfile | `pilot_v1/customide/frontend/Dockerfile` |
| Ollama standalone compose | `pilot_v1/customide/ollama/docker-compose.yml` |
| Backend chat route | `pilot_v1/customide/backend/app/routes/shared_llm.py` |
| Backend pane data | `pilot_v1/customide/backend/app/routes/runtime.py` |
| Frontend main JS | `pilot_v1/customide/frontend/js/app.js` |
| Frontend config | `pilot_v1/customide/frontend/js/config.js` |
| Autopilot script | `pilot_v1/scripts/worker_mtask_autopilot.sh` |
| Architecture snapshot | `pilot_v1/state/cockpit_architecture.md` |
| Worker config | `pilot_v1/config/worker1_services.json` |

---

## How to Restart the Stack

```bash
cd /home/larieladmin/mide-pilot/pilot_v1/customide

# Full restart
docker compose down
docker compose up -d

# Individual service
docker compose up -d --force-recreate backend
docker compose up -d --force-recreate frontend
docker compose up -d --force-recreate ollama

# Check status
docker compose ps
curl http://localhost:5555/api/status/bundle | python3 -m json.tool
```

---

## Environment Notes
- **GPU:** NVIDIA RTX 5060 Ti — 16311 MiB VRAM
- **Ollama models volume:** Docker named volume `ollama` (shared with open-webui stack)
- **Backend→Ollama:** Uses `172.17.0.1:11434` (not `host.docker.internal` — had timeout issues)
- **State directory symlink:** `~/Documents/itheia-llm/MIDE` → `~/mide-pilot` (same inode)
- **Autopilot runs from:** `/home/larieladmin/Documents/itheia-llm/MIDE/pilot_v1/scripts/`

