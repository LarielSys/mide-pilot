# MTASK-2008 Local Context (Private)

## Scope
- Workspace: OLEGREEN cockpit + local bridge behavior tuning.
- Objective: Improve comprehension and verified connector routing while keeping local-only context out of Ubuntu cockpit lane.

## Privacy Rules
- Local-only note.
- Do not dispatch this task to `ubuntu-worker-01`.
- Do not push this task to remote git unless explicitly approved.
- Do not mirror this content to room chat.

## Current Focus
- Human-style connector responses.
- Correct intent routing for task checks and latest MTASK checks.
- Verified runtime health checks (autopilot/heartbeat) from real signals.
- Compact cockpit layout with collapsed registry by default.

## Operator Intent
Keep this as local working context so the system knows what lane we are in without exposing it to Ubuntu cockpit.
